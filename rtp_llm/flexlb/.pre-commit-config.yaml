# .pre-commit-config.yaml
repos:
  - repo: local
    hooks:

      - id: spotless-check
        name: Spotless Import Check
        entry: bash -c "ls"
        language: system
        pass_filenames: false
        stages: [pre-commit]
